# 个人手册 · <你的名字或代号>

> 这是你私有的状态载体。三个模块只输出增量，由你自己合并进来（PROTOCOL 附：三个模块的接口）。
> 本文件不上传、不共享。导出即备份。
> 所有数字的唯一来源是你的简历；手册里出现的数字必须能在简历里找到。
> 只对愿意对自己诚实的人有用：往这里写有利于自己的东西，训练的是别人，不是你。

## 0. 基本信息
- 职能：
- 目标岗位区间：（例：执行 / 策略 / 机制 / 参谋，对应 PROTOCOL A-2 表一"岗位区间"）
- 当前简历版本号：（口径唯一来源，见第 3 节）
- 登记口头禅：（供 B-5 计数；不登记默认"其实 / 就是 / 然后 / 的话"）
- 表达目标线：口头禅 ___ 次 / 3 分钟；单案例 ≤ ___ 秒；案例题总作答 ≤ ___ 分钟

## 1. 优势追踪（空表，由你自己在场次后填）

| 优势 | 首次确认场次 | 证据（带来源标签） | 累计出现 |
|---|---|---|---|
| | | | |

说明：模块 B 不写优势表（B-7），只有你自己往这里加。"进步信号"（有触发场景但未出现的劣势）不进这里，进第 2 节的状态列。

## 2. 劣势追踪表（模块 A-5 读前 3 条；模块 B-4 / B-7 写增量）

| 模式 | 首次出现场次 | 累计次数 | 最近一次 | 状态 |
|---|---|---|---|---|
| | | | | |

- 模式名只用 `taxonomy/cn/pitfalls.md` 里的名字；新发现先进第 7 节"待定观察"，不进表。
- 状态取值：`活跃` / `连续 N 场有触发场景未出现`（N 由你回填，B-4 判定"有触发场景但未出现"时 +1，一旦重复归零）。
- B-7 增量格式：`劣势追踪表：<模式> 累计 +1 / 新增` → 对应更新"累计次数""最近一次"，新增则加一行。

## 3. 口径一致性清单（空表，唯一来源是简历）

| 条目 | 简历口径（原文或数字） | 冲突记录：场次 / 本场说法 / 我的裁定 |
|---|---|---|
| | | |

- 只抄简历，不抄记忆。简历改版后此表同步改。
- B-7 增量格式：`口径冲突（如有）：手册写 X，本场说 Y，由用户裁定` → 写进第三列，裁定后同步改简历或改说法，二选一。

## 4. 场次记录（模块 B-7 新增行；模块 C-2 回填"实际"）

| 日期 | 岗位大类 | 轮次 | 面试官类型 | 预测 | 实际 | 备注 |
|---|---|---|---|---|---|---|
| | | | | | 待回填 | |

- B-7 增量格式：`场次记录新增：<日期> | <岗位大类> | <类型> | 预测：<…> | 实际：待回填`
- C-1 输入四选一：`通过` / `未通过` / `走到第 N 轮` / `未知`。"走到第 N 轮"填"通过"并把 N 写进备注。面试官反馈（如有）一句话写进备注。
- `未知` 也要记，未知本身是数据。

## 5. 校准记录（模块 C-2 预测与实际不一致时新增；模块 A-4 按岗位大类读取）

| 日期 | 岗位大类 | 预测 | 实际 | 最可能错在 | 用户备注 |
|---|---|---|---|---|---|
| | | | | | 【空位·思考漏洞】 |

- C-2 增量格式：`<日期> | 预测 | 实际 | 最可能错在：<模块 B 已写的那个信号> | 用户备注：【空位·思考漏洞】`
- "最可能错在"只能填模块 B 当场已经写下的那个信号，不能事后换。
- 诊断与校准并存：不因结果回去改 B 的诊断（C-3）。
- A-4 读取规则：同岗位大类若有"预判类型错误"的记录，本次置信度降一级。

## 6. 信号命中记录（模块 C-2 "命中 +1" 落在这里）

| 信号编号 | 命中次数 | 最近命中场次 |
|---|---|---|
| H1 | 0 | |
| H2 | 0 | |
| H3 | 0 | |
| H4 | 0 | |
| H5 | 0 | |
| 软信号（逐条列） | 0 | |

- 只给模块 B **预测依据里引用过的**信号记命中；表中其他为"是"的信号不记（C-2）。
- `未知` 不更新任何命中计数。
- 不做跨场统计、不算命中率（C-3：样本太小，统计是伪精确）。这张表只用来看"哪个信号在我身上真的出现过"。

## 7. 待定观察（不进表；模块 B-3 / B-4 写入，下次模块 A 参照）

- <日期> ｜ 未预判到的问题 / 疑似新模式 ｜ 来源标签 ｜ 观察到的可复现特征
-

- 同一条观察在 ≥2 场重复出现 → 你决定是否提交为新类型 / 新模式（`templates/zh/contribute-type.md`），提交后才可进第 2 节。

## 8. 质疑原文记录（模块 B-6 H4 写入；模块 A-2 表二附"高风险条目表"读取）

| 场次日期 | 被质疑的简历条目 | 质疑原文（[原话] / [笔记]） | 我当时的回答 | 呈现是否已调整 |
|---|---|---|---|---|
| | | | | 否 |

- 这是模块 A 判定"简历高风险条目"仅认的两种依据之一（另一种是前场原文与简历矛盾）。这里没记录，A 就输出"无记录"。
- 只记面试官的句式原文，不记你对他动机的猜测。

## 9. 表达计数记录（仅有转录时；模块 B-5 写入）

| 场次日期 | 总发言字数 | 口头禅计数 | 单案例最长 | 案例题总作答时长 | 反问确认次数 |
|---|---|---|---|---|---|
| | | | | | |

- B-5 只计数不解读，输出"本场 X / 手册上一场 Y / 目标线 Z"。目标线在第 0 节。
- 表达瑕疵不进结果预测（B-6）。这张表只服务于练习，不服务于判断。
